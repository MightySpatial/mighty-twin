# @mightyspatial/ui

Shared React UI primitives — Panel, Dialog, Toolbar button, AttributeTable —
used by every widget and by the three apps. Depends only on
`@mightyspatial/tokens` for styling.

Don't put widget-specific UI here; those live inside each
`packages/widgets/<name>/src/` alongside the widget's logic.
